{
  "name": "oh-scipe.github.io",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/prepareDist.js && node scripts/copyOptimizeImages.js && python3 build.py --output dist",
    "clean": "node scripts/prepareDist.js --clean-only",
    "preview": "python3 -m http.server --directory dist 8008",
    "verify": "python3 verify_seo.py dist"
  },
  "license": "ISC",
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
